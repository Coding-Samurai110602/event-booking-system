apiVersion: v1
kind: Service
metadata:
  name: status-service
  namespace: default
  labels:
    app: status-service
spec:
  selector:
    app: status-service
  ports:
    - name: http
      port: 80
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
